import * as internal from "events";

export interface Employee {
  id: number;
  name: string;
  email: string;
  jobTitle: string;
  phone: string;
  salary: string;
  jobSeniority: string;
  imageUrl: string;
  employeeCode: string;
}
